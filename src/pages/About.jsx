import React from 'react'

const About = ({ ref }) => {
    return (
        <section ref={ref} className="h-screen">
            Education and Hobbies
        </section>
    )
}

export default About